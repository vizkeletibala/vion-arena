# Unity Assets Root

Unity content for the Vitrial greybox prototype lives under `Assets/Game/`.

The existing `frontend/` and `backend/` browser-game stack remains in place for the current web/DevOps platform. Unity gameplay work should not modify those directories unless a task explicitly asks for platform integration.
